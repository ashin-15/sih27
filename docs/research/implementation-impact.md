# Research to engineering bridge

| Finding and category | Evidence | Affected assumption | Engineering consequence | Decision | Component and validation |
| --- | --- | --- | --- | --- | --- |
| FACT: geometric mode yields unknown semantics/motion. | E-001 | A painted semantic cell might appear to imply model inference. | Require explicit model provenance and oracle isolation. | Proposed in draft design; not approved. | T-002/T-003; point-order and label-access tests. |
| LITERATURE-BACKED CLAIM: official semantic and moving-object tasks are distinct. | E-004 | One semantic score would establish motion quality. | Evaluate semantic and motion outputs separately. | Proposed; not approved. | T-003/T-005; official evaluator plus fixtures. |
| MEASURED RESULT: synthetic current-slice run has no recorded misses. | E-003 | A 100 ms config is a full real-time guarantee. | Keep release gate false; require target full-path benchmark. | Current code already does so. | T-008; long representative benchmark. |
| FACT: no free-space output exists. | E-001 | Unobserved space is traversable. | Specify occupied/free/unknown and false-free validation before navigation use. | Proposed; D-003 open. | T-006; ray and occlusion fixtures. |
| LITERATURE-BACKED CLAIM: the official tasks separate semantic points, instances, 4D association and moving points. | E-006 | One model benchmark can validate the whole product. | Keep distinct acceptance evidence and evaluator adapters; do not fold tracker/obstacle claims into semantic mIoU. | Research conclusion; product thresholds still open. | T-002 to T-005; official evaluator plus task-specific fixtures. |
| LITERATURE-BACKED CLAIM: FRNet weights and code are available; Autoware exposes stage timings. | E-007/E-008 | A published model FPS proves Drishti latency. | Screen FRNet as a candidate and copy the measurement boundaries, not the implementation or performance claim. | Candidate only; D-001/D-002 open. | T-003/T-008; checkpoint and full-path experiment. |
| FACT: Autoware FRNet ONNX artifacts use 27 T4Dataset classes and sensor-specific inputs, while Drishti requires SemanticKITTI IDs 0..19. | E-045; screen 0020 | An available FRNet-branded ONNX file can be used as Drishti's point-semantic checkpoint. | Keep these artifacts as an instrumentation reference; obtain exact allowed SemanticKITTI weights or approve remapping/retraining before adapter work. | Candidate screen, not product selection; D-002/D-005 open. | O-001/O-002/T-003/T-008; class-map, point-order, quality and full-path latency tests. |
| LITERATURE-BACKED CLAIM: occupancy mapping distinguishes occupied/free/unknown with ray observations. | E-009 | A ground/height cell or no return is free. | Define sensor, pose, occlusion, conflict and stale policies before producing free evidence. | Proposed; D-003 open. | T-006; false-free and ray fixtures. |
| MEASURED RESULT: both 100-frame real replays missed 100 ms on every frame and the `mapping_ms` stage dominated measured process time. | E-011 | Current CPU path is ready for a 10 Hz model addition. | Profile `aggregate_cells` and audit costs before choosing optimization; keep any model optional in the timing study. | Development measurement target recorded; no algorithm selected. | T-008; repeated representative replay and accounting checks. |
| MEASURED RESULT: extracted dataset metadata has max 129,392 points per scan and approximately 104 ms median sequence cadence. | E-012 | The 150,000-point cap is too small for this extracted benchmark, or 100 ms is an achieved latency. | Use 130,000 points and 10 Hz as the current-machine development workload; retain cap headroom and measure, not assume, performance. | D-001 development profile only. | T-001/T-008; experiment 0001. |
| MEASURED RESULT: buffering audit outside a paced current-result loop still missed 2/100 and 3/100 deadlines on sequences 08/00, then 28/1,000 and 3/1,000 in longer runs. | E-044; experiment 0019 | Synchronous audit alone explains all current-path misses, or short-run p50 proves the zero-miss rule. | Keep the zero-miss gate failing; if the owner selects an in-process consumer, budget its exact receipt and a bounded lossless audit path separately. | Diagnostic research only; consumer decision and full-path gate remain open. | O-001/T-007/T-008; complete-path replay and output verification. |
| DESIGN DECISION: the CUDA-backed replay release gate moves from the Intel laptop to an identified NVIDIA host. | Decision 0002 and user choice on 2026-09-25; earlier laptop choice is preserved in the PRD decision log. | A CUDA path could be accepted on the Intel Arc laptop, or historical vrgrid timings prove Drishti's deadline. | Keep the Intel laptop as CPU reference; inventory a physical NVIDIA host and measure the approved complete-output replay there. Defer live sensor evidence. | D-001 platform family selected; physical host and other gates open. | T-001/T-008; CPU/CUDA parity and AC-008 replay benchmark. |
| DESIGN DECISION and MEASURED RESULT: release deadline is 100 ms per scan with zero misses; current paced geometric replay missed 100/100. | E-025; experiment 0010 and user decision in PRD D-001 | The current CLI's unpaced processing time is a capture-to-output gate, or the selected deadline is already met. | Preserve scheduled-arrival-to-publication timing and a failing check; optimize only with exact-output and complete-path evidence. | Deadline selected, current path fails; no release pass. | O-001/T-008; AC-008 full-path replay. |
| MEASURED RESULT: packed grouping integrated with overflow fallback preserved 100 sampled CLI outputs and lowered work p50, but still missed all paced deadlines. | E-026; experiment 0010 | Isolated experiment 0003 speed implies a 100 ms product pass. | Keep the exact grouping change and profile remaining stages under the paced check; verify longer/cross-configuration outputs. | O-010 in validation; O-001 still open. | `mapping.py`, T-008; full-path deadline and exact-output tests. |
| MEASURED RESULT: minimum-at projection preserved 100 sampled CLI outputs and reduced projection p50, but still missed all paced deadlines. | E-027; experiment 0010 | Faster projection alone meets the selected deadline. | Keep nearest-range/original-ID tie semantics and profile load, preprocessing and mapping before further optimization. | O-001 remains open. | `projection.py`, T-008; exact range-image tests and paced full-path replay. |
| MEASURED RESULT: ordered-ID validation removed a sampled load hotspot while preserving 200 exact outputs across sequences 00/08; all paced deadlines still missed. | E-029; experiment 0010 | Full hash uniqueness is necessary for already strictly increasing IDs on every scan. | Retain the unordered fallback and prioritize mapping/preprocessing next. Do not infer complete-path feasibility. | O-001 remains open. | `contracts.py`, T-008; ID contract tests and paced replay. |
| MEASURED RESULT: row-wise `np.unique` and its sort dominate current cell aggregation on the profiled scans. | E-013 | Optimizing ownership, digest or JSON output first would remove most mapping time. | Evaluate alternative key grouping with exact digest/ownership equivalence before any production change. | Research priority; algorithm not selected. | T-013; isolated benchmark then fresh CLI replay. |
| MEASURED RESULT: packed grouping preserved 60 sampled outputs and cut geometric `process` p50, but stayed above 100 ms. | E-014 | One optimization or a RAM queue makes this a verified 10 Hz product. | Integrate the algorithm only under the approved contract, then measure the full CLI path and ordered pipeline capacity. | T-013 research complete; product change pending. | T-013/T-014/T-008; long replay and full-path timing. |
| DESIGN PROPOSAL: durable scan commits can preserve acquired frames for replay while bounded workers process them. | E-015 and experiment 0003 capacity calculation | A software queue can absorb indefinite overload while keeping outputs fresh. | Prototype local WAL ingress, bounded shared-memory scheduling, idempotent recovery and explicit stale/overload status. Freeze the source retry and stop policy. | Proposal only; no runtime implementation. | T-014/D-001/D-004; crash, disk-full and paced 10 Hz tests. |
| MEASURED RESULT: an isolated SQLite WAL/FULL recorder committed and reverified 100 paced scans on ext4 with a 23.98 ms worst observed read-through-commit time. | E-016 | Durable ingress necessarily consumes the full 100 ms interval on this laptop. | Continue with fault and concurrent-load tests; keep storage acknowledgement separate from processing completion. | Technology candidate only; no runtime integration or power-loss claim. | T-014; recovery, checkpoint, storage and full-path tests. |
| MEASURED RESULT: C++ and Python WAL/FULL recorders both reverified 100 scans and kept their short 10 Hz schedule under concurrent CLI load, with similar recorder latency; the CLI still missed every 100 ms budget. | E-017 | Changing recorder language will solve current processing throughput. | Prototype the durable writer in the existing Python ecosystem first; use native C++ for sensor SDK or an exact-output compute hotspot when measured. Require backlog-age and sustained full-path gates. | Research recommendation only; no runtime integration. | T-014/T-008; long concurrent runs and native compute comparison. |
| MEASURED RESULT: one-scan MCAP files acknowledged quickly, while ten-scan segments waited nearly 1 s; zstd compressed storage but added CPU tail. | E-017 | An MCAP append is automatically a durable per-scan acknowledgement. | Define a flush/seal/repair boundary before considering MCAP as ingress; retain it as an archival/interchange candidate. | Candidate only. | T-014; multi-topic contract and long-run storage tests. |
| MEASURED RESULT: injected SQLite process kill and page-capacity limit retained only committed rows. | E-018 | This demonstrates physical disk or power-loss safety. | Add physical fault and restart tests, source retry and duplicate handling, and explicit storage-failure status. | Research only. | T-014; no-loss acceptance under actual failure modes. |
| MEASURED RESULT: LMDB had lower commit tails than SQLite in three short local rounds and a matched 1,000-scan pair; a custom log varied markedly. LMDB passed injected faults and a 500-scan run with an independent reader/current replay. | E-020/E-021/E-023/E-024 | The earlier SQLite preference is a final technology decision, or the fastest short run proves a custom log is safe. | Advance a synchronous LMDB writer and short-lived reader prototype with explicit map/retention and source-ack contracts; retain SQLite as a fallback and avoid adopting the incomplete custom log. | Research preference revised; no accepted ADR or product integration. | T-014; longer concurrent duration, map growth, retention, source ack, physical-fault and output-age gates. |
| MEASURED RESULT: a separate scheduled producer preserved 100 exact frame outputs through a bounded two-frame queue, but filled it 93 times and missed every 100 ms output deadline. | E-037; experiment 0013 | A small in-memory queue lets the current geometric worker serve independent 10 Hz arrivals within the release deadline. | Keep queue-full, producer lag and output age as distinct gates; require faster service and an explicit overload contract before production scheduling. A volatile queue does not establish durable no-loss capture. | Current-path hypothesis rejected; no product scheduler selected. | O-001/O-011/T-014/T-008; approved full-path paced replay and durable source failure tests. |
| MEASURED RESULT: direct all-unknown cell evidence construction preserved 250 sampled geometric/oracle outputs and reduced mapping p50 on both tested geometric sequences, but all paced deadlines still missed. | E-038; experiment 0014 | Geometric unknown semantics require the full class histogram and tie calculation per scan. | Keep the exact shortcut while retaining the generic path for nonzero semantic/motion input; continue complete-path profiling and deadline validation. | Current-slice optimization integrated, no release pass. | `mapping.py`, O-001/T-008; varied real replay and full-path AC-008 gate. |
| MEASURED RESULT: reusing already immutable full-frame point and ID arrays preserved 250 sampled geometric/oracle outputs and lowered preprocessing time, but the strict deadline still missed 100/100 and 82/100 scans. | E-040; experiment 0016 | Full acceptance requires new filtered and immutable copies of existing frame arrays. | Reuse validated `ScanFrame` arrays when accepted indices cover all points; retain copies for filtered scans and preserve output checks. | Current-slice optimization integrated, no release pass. | `pipeline.py`, O-001/T-008; filtered fixtures and full-path AC-008 gate. |
| FACT: current frame JSONL contains audit digests/metrics, while Rerun carries only a selected visualization of the map. | E-041; O-001 output-boundary audit | The JSONL flush is complete machine-map publication for the 100 ms gate. | Freeze a versioned first-release payload, consumer and handoff event before attaching release meaning to the clock; retain the existing check as diagnostic evidence. | Product decision open under D-001/D-005; no external machine-output implementation. | O-001/T-001/T-007/T-008; schema and per-frame handoff tests. |
| MEASURED RESULT: current snapshot stored ZIP/NPY uses 15.64 MB median per scan with 13.59 ms encoding, while deflate level 6 uses 0.765 MB with 261.96 ms encoding. | E-042; experiment 0017 | Persisting a complete current snapshot is equivalent in cost to the digest JSONL report. | Budget serialization, disk and acknowledgement separately if persisted output is selected; evaluate other encodings before choosing one. Preserve in-process handoff as a review option. | Research screen only; no output format selected or runtime writer added. | O-001/T-001/T-007/T-008; complete-payload paced gate after contract approval. |
| FACT and MEASURED RESULT: installed `estimateGround` holds the GIL; a controlled Python heartbeat did not progress during 48 native calls. | E-046; experiment 0011 GIL follow-up and probe 0021 | A Python thread pool alone enables ground segmentation to overlap Python-driven projection/grouping. | Before adopting threaded overlap, review an explicit native GIL-release boundary or test a persistent separate process. Keep each adaptive estimator exclusively owned and ordered; verify exact outputs and end-to-end tails afterward. | Verification only; no binding, dependency or scheduler change approved or implemented. | `ground.py`, O-001/T-008; binding safety, heartbeat controls, parity and paced replay. |

Every future significant finding should identify the changed assumption, proposed decision, task and validation. Failed experiments may invalidate an ADR and reopen its question.
