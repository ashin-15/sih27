# Open items

Updated 2026-09-25. This register records blockers and next evidence, not promises of delivery.
Close an item only with a linked decision, implementation, and validation record.

| ID | Status | Owner or decision | Open item | Next evidence |
| --- | --- | --- | --- | --- |
| O-001 | IN PROGRESS | Product owner and engineering, D-001/D-005 | [Decision 0002](decisions/0002-cuda-replay-release-platform.md) moves the strict 100 ms, zero-miss CUDA replay gate to an identified NVIDIA host; the user now selects this laptop CPU for active implementation, with optional CUDA support later. NVIDIA access must not block approved CPU-only work; GPU validation is deferred. An optional CuPy projection, ownership and reduction path is integrated, with CPU array API parity checks but no GPU parity or timing result. The paced checker now uses a synchronous receipt for the complete *current single-frame* in-memory result, with report flush age separate; this is diagnostic, not the approved first-release output. Rerun is outside the 100 ms check. Earlier geometric replay still missed the deadline: the minimal in-process 1,000-frame screen missed 28/1,000 scans on 08 and 3/1,000 on 00 (E-044), before model, temporal stages and complete machine output exist. The JSONL is audit only. E-046 verified that installed Patchwork++ holds the GIL, so a Python thread pool alone does not enable its Python-driven stage overlap; safe GIL release or process isolation and speedup remain unverified. The [CUDA goal](o-001-cuda-frame-path.md) and [release brief](o-001-release-profile.md) capture the remaining proof contract. | Consumer selected: same-process evaluator receipt for the complete versioned immutable result; persist receipts/audit, with full-payload disk persistence outside this endpoint. Evidence-only output selected; workload/window/cap approval explicitly deferred. Freeze payload schema and remaining D-001/D-005 resource/quality gates; validate CPU/CUDA parity and complete-path timing on an NVIDIA host; prove AC-008. See the dated handoff review in [work log](work-log.md#2026-09-25---handoff-reconciliation-and-partial-output-contract). |
| O-002 | BLOCKED | Product owner, D-002 | Model strategy, allowed checkpoint/license, training split, semantic quality gate, and deployment format are unresolved. FRNet screen E-045 found Autoware's 27-class T4Dataset artifacts are not a direct SemanticKITTI checkpoint. | Obtain and inspect exact allowed SemanticKITTI weights, compare candidates under RQ-001, and approve the model milestone before T-003. |
| O-003 | BLOCKED | Product owner, D-003/D-005 | Evidence-only output is selected; planner/navigation-safe use is deferred. Numeric false-free, unknown/stale handling and recovery criteria remain unresolved. | Freeze evidence-quality and recovery gates; run RQ-003 fixtures before T-006. |
| O-004 | BLOCKED | Product owner, D-004 | Live sensor input and planner-facing output are deferred beyond the first replay release. Future sensor, calibration, deskew and navigation-safety contracts remain unresolved. | Reopen only for an explicitly approved later live/planner milestone; define its contracts before T-009. |
| O-005 | TODO | Engineering, T-002 to T-006 | No learned inference, detector, tracker, measured motion, temporal state, or free-space proof exists in the active path. | Implement only after T-001 approval; validate each acceptance criterion. |
| O-006 | TODO | Engineering, T-008 | Complete-path real-time and memory guarantees are unmeasured. | Benchmark representative long sequences on target hardware with all enabled stages. |
| O-007 | IN PROGRESS | Research, RQ-001/RQ-004 | FRNet remains a point-semantic candidate. Screen E-045 rejected Autoware's 27-class T4Dataset ONNX artifacts as a direct SemanticKITTI match; the authors' exact SemanticKITTI files, terms, preprocessing and laptop latency remain unverified. | Obtain and pin the authors' checkpoint or another allowed candidate, then compare class alignment, held-out quality and complete-path latency under frozen contracts using `research/model-candidates.md`. |
| O-008 | DONE | Evidence owner | Earlier report files remain absent, but the proposed model document now cites two new provenance-complete 100-frame real-data reports in unique run directories. | Preserve reports and do not reinterpret them as full-path or safety evidence. |
| O-009 | TODO | Project owner | CI, deployment, release, security review, and ownership policy are not established in this repository. | Define policy once target environment and acceptance contract are known. |
| O-010 | VALIDATION | Engineering, RQ-004 | Packed int64 grouping with overflow fallback is integrated. Edge tests pass; fresh 100-frame CLI replays on sequences 08 and 00 matched all prior input/map digests, cell counts and accounting. The grouping-only run cut audited work p50 to 193.4 ms but still missed 100 ms; later exact-output changes reduced it further. Longer sequence and cross-configuration validation remain. | Validate exact outputs and resource/latency behavior over longer and varied real sequences; profile the remaining preprocessing and mapping costs. |
| O-011 | IN PROGRESS | Product owner and engineering, T-014 | Continuous 10 Hz arrival exceeds the current-path service rate. Experiment 0009 compared SQLite, LMDB and a custom log; LMDB showed lower commit tails than SQLite in short runs and a matched 1,000-scan pair, and passed injected process-kill/map-limit checks. Both long runs reverified all payloads, but source-read variation made LMDB's long-run acquisition p95 slower. A 500-scan LMDB writer with reader/current replay also passed, while replay missed 100/100 processing deadlines. A separate scheduled producer with a two-frame volatile queue preserved 100 frames in one diagnostic run but blocked on 93 queue-full events and fell behind its 10 Hz schedule. No runtime ingress, live-sensor retry contract, disk budget, stale-output policy or measured parallel scheduler exists. | Freeze source ack/retry and overload/freshness behavior; test longer concurrent workers/readers, map growth and retention, physical disk/power faults and complete-path output age. |

See `execution-plan.md` for task sequence and `research/modules.md` for research paired to tasks.
The [O-001 contract proposal](o-001-contract-proposal.md) now gives D-001/D-005 a concrete
payload, evaluator, replay, audit and threshold worksheet for review. It does not resolve
the deferred workload/cap, model, numeric quality/resource limits or implementation approval.
The first reproducible current-path measurements and remaining limits are in
`research/experiments/0001-replay-baseline-protocol.md`.
